{{#each model as |analysis|}}
  <div>{{analysis.title}}</div>
  <div>{{analysis.dataset.id}}</div>
  <div>{{analysis.dataset.name}}</div>
  {{#link-to "analyses.view" analysis.id}}view{{/link-to}}
{{/each}}